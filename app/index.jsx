import { Text } from 'react-native';

//No use for this screen, app never renders
export default function Index() {
  return(<Text>Layout management gone wrong. This should not be displayed</Text>)
}
